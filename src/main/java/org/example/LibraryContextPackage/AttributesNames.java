package org.example.LibraryContextPackage;

/**
 * Set of names matching CommonUsers, Admins and Books attributes.
 */
public enum AttributesNames {
        login, password, name, surname, mail, booksNr, author, category, available, returnDate, user_id;
}
